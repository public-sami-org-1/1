name: Aqua Trivy

on:
  pull_request:

jobs:
  aqua:
    runs-on: ubuntu-latest

    steps:
      - name: Aqua scanner
        uses: tamirsinai/shared-workflows/.github/workflows/aqua.yml@main
        secrets: inherit
